<div class = "room-item">
    <h3>登録施設一覧</h3>


    <table>
        <tr>
            <th>施設名</th>
            <th>紹介文</th>
            <th>料金（1人/1泊）</th>
        </tr>
        <% @rooms.each do |room| %>
        <tr>
          <% if room.user_id == current_user.id %>
            <td><%= room.name %></td>
            <td><%= room.detail %></td>
            <td><%= room.price %>円</td>
          <% end %>
        </tr>
        <% end %>
    </table>
